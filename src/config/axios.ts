/**
 * axios 默认配置
 */
export const AXIOS_DEFAULT_CONFIG = {
  timeout: 20000,
};

export const API_DEFAULT_CONFIG = {
  mockBaseURL: '',
  mock: true,
  debug: false,
  sep: '/',
};

export const CONST_DEFAULT_CONFIG = {
  sep: '/',
};
